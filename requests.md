### Creating requests
`r = request.get('www.google.com')`

### Convert request to json
`r.json()`

### Parsing json
**Get direct children**
`variable = json['child_name']`

### Status codes
`r.status_code`
